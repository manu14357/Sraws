import React from 'react';
import ReactMarkdown from './ReactMarkdown'; // Adjust the path as per your file structure

const App = () => {
  return (
    <div>
      <ReactMarkdown />
    </div>
  );
};

export default App;
